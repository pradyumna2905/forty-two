-# = form_tag search_path, method: :get, class: "form-inline my-2 my-lg-0" do
-#   = text_field_tag :w, params[:w], class: "form-control mr-sm-2", placeholder: "Search a definition"
-#   = submit_tag 'Search', class: "btn btn-outline-dark my-2 my-sm-0"

= form_tag search_path, method: :get, class: "d-flex mx-5" do
  = text_field_tag :w, params[:w], class: "form-control mr-sm-2", placeholder: "Search a definition"
  = submit_tag 'Search', class: "btn btn-outline-dark my-2 my-sm-0"


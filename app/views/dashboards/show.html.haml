%h1.text-center.my-2 History
- @entries.each do |entry|
  .col-6.offset-md-3
    .card
      .card-header= entry.word
      - entry.definitions.each do |definition|
        .card-body
          %p= definition

